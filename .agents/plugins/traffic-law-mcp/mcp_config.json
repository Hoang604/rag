{
  "mcpServers": {
    "vietnamese-traffic-law": {
      "command": "uv",
      "args": [
        "run",
        "rag-eval",
        "legal-server",
        "--log-file",
        "logs/mcp_server.log"
      ],
      "env": {
        "DATABASE_URL": "postgresql://postgres:postgres@localhost:15432/rag_legal"
      }
    }
  }
}
